# This script is used to test the database connection and print the columns of the Orders table.
from db import create_connection
conn = create_connection()
cursor = conn.cursor()
cursor.execute("SHOW COLUMNS FROM Orders")
cols = cursor.fetchall()
print("Orders table columns:")
for col in cols:
    print(" ", col[0])
cursor.close()
conn.close()

